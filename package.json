{
  "name": "carenova",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "vercel-build": "npm run build"
  }
}
