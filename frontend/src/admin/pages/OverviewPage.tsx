import React from 'react';
import AdminComingSoon from '../components/AdminComingSoon';

// Placeholder — replaced by the real KPI/trend/attention-list build in
// Bölüm C.1 (next commit after the host-architecture scaffold in Bölüm B).
export default function OverviewPage() {
  return (
    <AdminComingSoon
      title="Genel Bakış"
      description="Platform KPI'ları (aktif klinik, MRR, ortalama ilk yanıt süresi), 30 günlük trend ve dikkat gerektirenler listesi burada olacak."
    />
  );
}
